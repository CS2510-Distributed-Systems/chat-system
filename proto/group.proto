syntax="proto3";

option go_package = "./pb";

package chat;
//import "user.proto";
//import "message.proto";

message Group {
    string groupID =1;
    string groupname =2;
    map<string, string> participants = 3;
    map<string, string> messages =4;
    map<string, int32> Likes = 5;
}