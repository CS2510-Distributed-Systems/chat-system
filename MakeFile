CHAT_DIR = chat-system

gen:
	protoc --proto_path=proto proto/*.proto --go_out=. --go-grpc_out=require_unimplemented_servers=false:.

clean:
	rm ./pb/*.go

server:
	go run cmd/server/main.go

client:
	go run cmd/client/main.go --addr=0.0.0.0 --port=12000

test:
	go test -cover  ./...